Create a first class
Create a method
initialize a three variables(n1,n2,n3) and declare a values to two variables(n1=0,n2=1)
print the n1,n2 values then,
Declare a new variable inside a for loop and store the number
initialize variable is 1(for (int i=1;i<n;i++))
Add a n1&n2 values and storing a value in n3 then,
print n3 value
now, n1=n2
n2=n3
Then Print fibonacci numbers from 1 to n.
Again create a second class
Taking a user input from scanner class
Reading a character and storing in a integer type variable
Using first name calling a method
Then get the output
Output:
  n=5
  0,1,1,2,3